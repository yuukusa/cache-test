data CacheItem {
    int key
    int value
}

interface Cache {
    const int MAX_SIZE = 10
    transfer CacheItem content[]
   // transfer int pointer // points to where to include the next item

    int get(int key)
    //bool update(store CacheItem item)
    void add(store CacheItem item)
    void printCache()
}
